# Game Items Store

A small store for digital game items: a Django REST API and a React web client.

- `backend/` — Django + DRF API (JWT auth, product catalog, orders)
- `frontend/` — React + TypeScript client built with Vite

Setup instructions live in each module's README. Full docs are added as the project takes shape.
